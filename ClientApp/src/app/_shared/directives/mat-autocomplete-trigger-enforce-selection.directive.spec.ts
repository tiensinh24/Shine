import { MatAutocompleteTriggerEnforceSelectionDirective } from './mat-autocomplete-trigger-enforce-selection.directive';

describe('MatAutocompleteTriggerEnforceSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new MatAutocompleteTriggerEnforceSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
